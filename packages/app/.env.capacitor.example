# Capacitor build environment variables
# Copy this file to .env.capacitor and replace YOUR_IP with your machine's local IP address.
# Used when building with: npm run build:cap (loads via --mode capacitor)

VITE_API_URL=http://YOUR_IP:4000/api/v1
VITE_SERVER_URL=http://YOUR_IP:4000

# For production builds, use your deployed backend URL instead:
# VITE_API_URL=https://api.yourapp.com/api/v1
# VITE_SERVER_URL=https://api.yourapp.com
