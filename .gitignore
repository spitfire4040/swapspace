node_modules/
dist/
build/
.env
*.log
.DS_Store

# MCP config (contains local paths)
.mcp.json

# Capacitor native projects (generated, large binaries)
packages/app/ios/
packages/app/android/

# Capacitor env (contains local IP)
packages/app/.env.capacitor

# Claude Code local settings
.claude/settings.local.json
